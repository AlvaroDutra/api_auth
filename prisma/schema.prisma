generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./userDb.db"
}

model User{
  Id String @id @default(uuid())
  name String 
  email String @unique
  password String 
}
